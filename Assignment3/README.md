# Assignment 3: Block-wise SVD Image Compression

## How to Run (Jupyter Notebook)

1. Make sure the following files are in the same folder:
   - `Assignment_3.ipynb`
   - `baboon.jpg`

2. Open and run the notebook:
   - **Jupyter Notebook / JupyterLab**: open `Assignment_3.ipynb` and run all cells (Kernel → Restart & Run All is recommended).
   - **Google Colab**: upload `Assignment_3.ipynb` and `baboon.jpg`, then run all cells.

## Requirements

The notebook uses:
- `numpy`
- `matplotlib`
- `Pillow` (PIL)

If you need to install them, run the following inside the notebook:

```python
%pip install numpy matplotlib pillow
