# Assignment 4: Customer Churn Prediction with XGBoost

## How to Run (Jupyter Notebook)

1. Make sure the following files are in the same folder:

   - `xgboost-starter.ipynb`
   - `telco-customer-churn-by-IBM.csv`

2. Open and run the notebook:

   - Jupyter Notebook / JupyterLab: open `xgboost-starter.ipynb` and run all cells
   - VS Code: open `xgboost-starter.ipynb`, select a Python kernel, and run all cells
   - Google Colab: upload `xgboost-starter.ipynb` and `telco-customer-churn-by-IBM.csv`, then run all cells

## Requirements

The notebook uses:

- `pandas`
- `numpy`
- `scikit-learn`
- `xgboost`
- `matplotlib`

If you need to install them, run:

```python
%pip install pandas numpy scikit-learn xgboost matplotlib
